{
  "name": "companion-bot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute companion-db --file=./migrations/001_initial.sql",
    "db:migrate:local": "wrangler d1 execute companion-db --local --file=./migrations/001_initial.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  },
  "dependencies": {
    "stripe": "^17.5.0"
  }
}